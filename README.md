# PawGuardian - Pet Safety Agent 🐾

[![Deploy to Cloud Run](https://deploy.cloud.run/button.svg)](https://paw-guardian-576306226088.asia-northeast1.run.app/)

**車内に残されたペットの熱中症や分離不安を自律的に防ぐ、Agentic AI 監視システム**  
**An autonomous monitoring system powered by Agentic AI to prevent heatstroke and anxiety for pets left in vehicles.**

<img width="1400" height="966" alt="image" src="https://github.com/user-attachments/assets/65355b1b-55f8-4185-b91a-309f4b28d897" />

---

## 🌟 概要 / Overview

**PawGuardian** は、Google Cloud の **Vertex AI Gemini 2.5 Flash** を搭載した「自律型エージェント」です。単なる監視カメラではなく、リアルタイムの動画解析とセンサーデータを組み合わせ、ペットの異常を検知した際に自律的に介入（通報・音楽再生・環境操作）を行います。

---

## 🤖 システムアーキテクチャ：ReAct パターンによる 2 つのエージェント

PawGuardian は **Agentic Reasoning (ReAct パターン)** を採用し、以下の 2 つのエージェントで構成されています：

### Agent 1: Observer (視覚分析エージェント)

**役割**: 動画から客観的な事実を抽出し、ペットの状態を判定

**検出項目**:
- ✅ ペットの有無（**早期終了ロジック**: ペットが検出されない場合、即座に監視を中止して API コストを削減）
- 📊 不安レベルの 3 段階分類

**不安レベルの具体的な定義**:

| レベル | 状態 | 行動特性 |
|-------|------|--------|
| **Relax** | 安全 | 静かに座っている、探索行動なし、体がリラックス |
| **Low Anxiety** | 注意 | 鼻をなめる、耳が後ろへ、落ち着きなくそわそわ |
| **High Anxiety** | 警告 | 窓をひっかく、激しい息遣い、継続的な吠え |

**出力例** (JSON フォーマット):
```json
{
  "subject_detected": true,
  "anxiety_level": "Low",
  "observations": "犬は不安そうに車内を見回っています",
  "stress_signs": ["鼻をなめる", "そわそわした動き"]
}
```

**早期終了の流れ**:
```python
if not obs_data.get("subject_detected"):
    # ペットが検出されない場合
    → 警告メッセージ表示
    → 監視完了（Agent 2 は実行されない）
    → 処理中止 (st.stop())
    → API コスト削減 ✅
```

---

### Agent 2: Decision Maker + Tool Executor (判断と実行エージェント)

**役割**: Observer の結果、ペット情報（犬種・年齢・既往症）、車内温度を統合して、リスク評価と対応アクションを決定・実行

**判断アルゴリズム（5 つのセーフティプロトコル）**:

```
Protocol 1: 高温危機
  IF subject_detected = true AND 温度 > 35°C
  → アクション: 【通報】+ 【窓を全開】

Protocol 2: 短頭種特別対応
  IF subject_detected = true AND 犬種 = Brachycephalic AND 温度 > 30°C
  → アクション: 【通報】

Protocol 3: 高度の不安
  IF subject_detected = true AND 不安レベル = 'High'
  → アクション: 【通報】

Protocol 4: 軽度の不安（ケア対応）
  IF subject_detected = true AND 不安レベル = 'Low'
  → アクション: 【リラックス音楽再生】+ 【SMS 通知】

Protocol 5: 安全状態（非介入）⭐ 重要
  IF subject_detected = true AND 不安レベル = 'Relax' AND 温度 < 30°C
  → アクション: なし（最終レポート: "ペットは安全です"）
```

**重要**: Protocol 5 により、不要な介入と API コストの無駄を防止

---

## 🔧 4 つのアクション実行ツール

Agent 2 の判断に基づき、以下の **4 つのツール** が自動的に実行されます：

| ツール | 機能 | API | 用途 |
|-------|------|-----|------|
| **send_sms_alert** | SMS 送信 | Twilio Messages | 軽度の不安を飼い主に通知 |
| **make_emergency_call** | 緊急通話 | Twilio Voice | ペットの緊急時に飼い主に通報（日本語音声） |
| **open_car_windows** | 窓開放制御 | 予約可能 | 高温時に車内温度を低下 (0-100%) |
| **play_music** | 音楽再生 | Pixabay Audio | リラックス音楽またはホワイトノイズで不安を軽減 |

**ツール呼び出しの例**:
```python
# 低度の不安が検出された場合
send_sms_alert(message="ペットが不安そうです")
play_music(track_type="リラックス")

# 高温+検出時
make_emergency_call(message="車内温度が35度を超えています")
open_car_windows(level=100)
```

---

## ✨ 主な特徴 / Features

### 1. **自律的な意思決定**
- マルチモーダル入力（動画 + 温度センサー）による高度なリスク判断
- JSON ベースの構造化出力で、エージェント間の情報伝達を最適化
- **Temperature = 0.0** での決定性推論により、安全性を最大化

### 2. **コスト・安全性の最適化**
- **早期終了ロジック（Early Exit）**: ペットが検出されない場合、Agent 2 を実行せず API コストを削減
- **プロトコル 5**: 安全状態では一切の介入を行わない
- 結果: 不要な API 呼び出しを最小化しながら、真の危機には迅速に対応

### 3. **パーソナライズされた安全基準**
飼い主が以下をカスタマイズ可能：

| 設定項目 | 範囲 | デフォルト | 説明 |
|--------|------|---------|------|
| ペット名 | 任意 | "Lucky" | ペットの識別名 |
| 犬種 | 複数選択 | コーギー | 犬種別の自動調整あり |
| 年齢 | 0.5-20 歳 | 4.5 | シニア犬判定（>10 歳）に使用 |
| 体重 | 5-30 kg | 13.5 | リスク評価の参考値 |
| 既往症 | テキスト | 空 | 個別リスク要因（例: 気管虚脱） |
| **不安感度** | 1-10 | 8 | **ユーザーの感度設定** |

**自動調整ルール**:
- 🐕 **短頭種** (フレンチブルドッグ、パグ): 温度閾値 **-5°C** (35°C → 30°C)
- 👴 **シニア犬** (年齢 > 10 歳): 不安反応の検知感度 **UP**

### 4. **セキュア設計**
- **Google Secret Manager**: Twilio API キーなどの秘匿情報を安全に管理
- **Signed URL (V4)**: IAM 認証による Google Cloud Storage 上の動画へのセキュアアクセス
- **サービスアカウント権限**: 最小権限の原則に基づいた IAM 設定
- **CORS 設定**: Google Cloud Storage 上の動画ストリーミングに対応

---

## 🛠 技術スタック / Tech Stack

| レイヤー | 技術 |
|--------|------|
| **Frontend/UI** | Streamlit (Python Web Framework) |
| **AI エンジン** | Vertex AI Gemini 2.5 Flash (asia-northeast1 リージョン) |
| **LLM 設定** | Temperature = 0.0（決定性推論） |
| **クラウド基盤** | Google Cloud Run, Cloud Storage, Secret Manager |
| **通信 API** | Twilio Voice API, Twilio SMS API |
| **デプロイ** | Docker (Python 3.11-slim) |
| **依存ライブラリ** | google-cloud-aiplatform, google-cloud-storage, twilio, python-dotenv |

---

## 📊 実行方法 / Setup & Execution

### Prerequisites / 前提条件

- Google Cloud プロジェクト（Vertex AI API, Secret Manager API を有効化）
- Twilio アカウント（無料トライアル可）
- Python 3.11 以上
- gcloud CLI

---

### 2️⃣ ローカル開発環境の構築

#### 🍎 macOS の場合

```bash
# リポジトリをクローン
git clone https://github.com/song4wk/PawGuardian.git
cd PawGuardian

# Python 仮想環境を作成
python3 -m venv venv
source venv/bin/activate

# 依存パッケージをインストール
pip install --upgrade pip
pip install -r requirements.txt

# Google Cloud 認証
gcloud auth application-default login

# アプリケーション起動
streamlit run app.py
```

#### 🪟 Windows の場合

```powershell
# リポジトリをクローン
git clone https://github.com/song4wk/PawGuardian.git
cd PawGuardian

# Python 仮想環境を作成
python -m venv venv
.\venv\Scripts\activate

# 依存パッケージをインストール
pip install --upgrade pip
pip install -r requirements.txt

# Google Cloud 認証
gcloud auth application-default login

# アプリケーション起動
streamlit run app.py
```

#### 🐧 Linux の場合

```bash
# リポジトリをクローン
git clone https://github.com/song4wk/PawGuardian.git
cd PawGuardian

# Python 仮想環境を作成
python3 -m venv venv
source venv/bin/activate

# 依存パッケージをインストール
pip install --upgrade pip
pip install -r requirements.txt

# Google Cloud 認証
gcloud auth application-default login

# アプリケーション起動
streamlit run app.py
```

---

### 3️⃣ Docker で実行

```bash
# イメージをビルド
docker build -t paw-guardian .

# コンテナを実行
docker run -p 8080:8080 \
  -e GOOGLE_APPLICATION_CREDENTIALS=/app/service_account.json \
  -v ~/.config/gcloud:/root/.config/gcloud \
  paw-guardian
```

---

### 4️⃣ Google Cloud Run へのデプロイ

```bash
# Cloud Run にデプロイ
gcloud run deploy paw-guardian \
  --source . \
  --platform managed \
  --region asia-northeast1 \
  --allow-unauthenticated \
  --memory 2Gi \
  --timeout 600 \
  --set-env-vars GOOGLE_CLOUD_PROJECT=sentientcarguard
```

デプロイ後、以下のエンドポイントでアクセス可能：
```
https://paw-guardian-xxx.asia-northeast1.run.app
```

---

## 📊 実行フロー図

```
┌─────────────────────────────────────────────────────────┐
│                   入力データ                             │
│         • ビデオストリーム                               │
│         • 車内温度（スライダー）                         │
│         • ペット情報（名前、犬種、年齢など）              │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Agent 1: Observer    │
        │  (視覚分析)          │
        └──────────────┬───────┘
                       │
          ┌────────────▼────────────┐
          │  ペット検出？           │
          └────────────┬────────────┘
                   ┌───┴────┐
                  NO        YES
                   │        │
                   ▼        ▼
            ┌─────────┐  ┌────────────────────┐
            │ Early   │  │ Agent 2: Decision  │
            │ Exit    │  │ Maker + Executor   │
            │ (停止)  │  │ (判断 & 実行)      │
            └─────────┘  └────────┬───────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   5 つのセーフティプロトコル  │
                    │   Protocol 1-5 を評価     │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │    必要なツールを実行      │
                    │ • send_sms_alert          │
                    │ • make_emergency_call     │
                    │ • open_car_windows        │
                    │ • play_music              │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   ReAct ループ             │
                    │   (結果をモデルに返却)     │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │  最終レポート（日本語）    │
                    │  ユーザーに結果を表示      │
                    └─────────────────────────────┘
```

---

## 💡 実装のポイント

### ReAct ループ（反復推論と行動）

```python
# Step 1: ユーザーメッセージを送信
response = chat.send_message(user_msg, generation_config={"temperature": 0.0})

# Step 2: ツール呼び出しを抽出して実行
for call in response.candidates[0].function_calls:
    result = execute_tool(call.name, call.args)
    function_responses.append(Part.from_function_response(...))

# Step 3: 実行結果をモデルに返却
final_res = chat.send_message(function_responses)

# Step 4: 最終レポート生成
print(final_res.text)  # 日本語で結果を表示
```

### JSON の自動クリーニング

Model からのレスポンスに含まれるマークダウンバッファを削除：

```python
def clean_json_text(text):
    """モデル出力から JSON を抽出"""
    text = re.sub(r"```json\s*", "", text, flags=re.IGNORECASE)
    text = re.sub(r"```\s*$", "", text, flags=re.MULTILINE)
    start = text.find("{")
    end = text.rfind("}")
    return text[start : end + 1] if start != -1 else text
```

### 言語の統一

すべてのプロンプトと出力を日本語で統一：

```python
[Language Rule]
- ALL your responses (Thought and Final Report) MUST be in JAPANESE.
- Even if the tool execution results are in English, 
  you must summarize them in JAPANESE.
```

---

## 📁 ファイル構成

```
PawGuardian/
├── app.py                 # メインアプリケーション（Streamlit）
├── requirements.txt       # Python 依存関係
├── Dockerfile             # Docker イメージ定義
├── cors.json              # Google Cloud Storage CORS 設定
├── README.md              # このファイル
└── service_account.json   # ⚠️ ローカル開発用（.gitignore で無視）
```

---

## 🧪 テスト用デモシナリオ

アプリケーション内で 4 つのテスト動画を使用：

| シナリオ | 状態 | 検出内容 |
|--------|------|--------|
| **Scenario A** | 待機中（Relax） | ペット: ✅ 、不安: なし |
| **Scenario B** | 軽度の不安 | ペット: ✅ 、不安: Low |
| **Scenario C** | 重度の不安 | ペット: ✅ 、不安: High |
| **Scenario D** | 空席 | ペット: ❌ 、不安: なし |

各シナリオで以下を検証：
- ✅ 各 Agent の出力が正確か
- ✅ 適切なプロトコルが発動するか
- ✅ ツール実行が期待通りか

---

## 🔮 今後の拡張予定

- [ ] リアルタイム窓開放デバイスの統合（現在はシミュレーション）
- [ ] 心拍数・体温センサーの連携
- [ ] 複数ペット対応
- [ ] オーナー向けモバイルアプリ
- [ ] インシデント履歴のダッシュボード可視化
- [ ] Web カメラへのライブ接続
- [ ] 多言語サポート

---

## ⚠️ 注意事項

1. **Twilio API の料金**: SMS と通話は従量制です。開発時は試験電話番号を使用してください
2. **Google Cloud 料金**: Vertex AI API、Cloud Storage は使用量に応じて課金されます
3. **プライバシー**: 動画データは Google Cloud に送信されます。プライバシーポリシーを確認してください
4. **リアルタイム性**: ネットワーク遅延により数秒の遅延が発生する可能性があります

---

## 🤝 貢献 / Contributing

バグ報告、機能提案、プルリクエストを歓迎します！

```bash
# フォークしてブランチを作成
git checkout -b feature/amazing-feature
git commit -m "Add some amazing feature"
git push origin feature/amazing-feature

# Pull Request を作成
```

---

## 📚 参考資料

- [Google Cloud Vertex AI ドキュメント](https://cloud.google.com/vertex-ai/docs)
- [Gemini API 関数呼び出し（Function Calling）](https://ai.google.dev/docs/function_calling)
- [Streamlit ドキュメント](https://docs.streamlit.io/)
- [Twilio Python SDK](https://www.twilio.com/docs/python/install)
- [Google Cloud Secret Manager](https://cloud.google.com/secret-manager/docs)

---

## 📄 ライセンス

このプロジェクトは MIT ライセンスの下でライセンスされています。詳細は LICENSE を参照してください。

---

## 🐾 謝辞

このプロジェクトは **Google Cloud Agentic AI Hackathon** に参加したものです。  
ペット愛好家や AI/IoT 開発者の皆さんのご意見やフィードバックをお待ちしています！

**最後に、ペットたちの安全のため、このシステムの継続的な改善にご協力ください。**